import React, { useContext } from 'react';
import { AuthContext } from '../context/AuthProvider';

const Home = () => { 
    return (
        <div>
            This is home page
        </div>
    );
};

export default Home;